import 'package:flutter/material.dart';

class FavoritesPosts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('thie is user favorite posts'),
    );
  }
}
